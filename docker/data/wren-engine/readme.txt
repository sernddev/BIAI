## build the project ==wren-engine-0.15.1
generate single jar by running below mvn command 

>> mvn clean package -DskipTests -P exec-jar

rename the jar file to "wren-server-executable.jar" and place it here(~/data/wren-engine/wren-server-executable.jar)